# 🚁 Instant Wellness Kits: Tax Calculation & Analytics System

Цей репозиторій містить рішення тестового завдання "WEB Dev. Test Task" для компанії BetterMe. Проєкт є повноцінною внутрішньою системою (адмін-панель + API) для розрахунку податків на доставку товарів дронами у штаті Нью-Йорк, а також для генерації фінансової аналітики та візуалізації попиту.

## 🎯 1. Бізнес-проблема (Сценарій)
Сервіс миттєвої доставки "Instant Wellness Kits" зіткнувся з критичною проблемою: під час стрімкого масштабування компанія повністю проігнорувала питання стягнення податку з продажу (Sales Tax) з клієнтів. Компанія має терміново розрахувати `composite sales tax` для кожного історичного (та майбутнього) замовлення на основі GPS-координат (latitude, longitude) точки доставки.

## 💡 2. Прийняті бізнес- та архітектурні рішення

Згідно з рекомендаціями до завдання, головний фокус було зроблено на вирішенні бізнес-проблеми. Під час розробки ми ухвалили кілька ключових рішень:

### 2.1. Відхилення замовлень поза штатом (Out-of-State) та аналіз потенціалу
Оскільки ліцензія компанії на доставку дронами покриває **лише територію штату Нью-Йорк**, система автоматично відхиляє (`status: failed`) замовлення, координати яких знаходяться за межами штату (наприклад, у Нью-Джерсі).
**Однак ми зберігаємо ці дані у базі.** У фінансовому PDF-звіті виводиться метрика **"Missed Revenue (Out-of-State)"** — це втрачений дохід. Таке рішення дозволяє бізнесу відслідковувати попит у сусідніх регіонах та приймати data-driven рішення щодо доцільності розширення ліцензії на інші штати.

### 2.2. "Хто платить податок?" (Tax Owed)
За умовами задачі, клієнти вже сплатили лише `subtotal`, а `sales tax` з них не стягувався. Це означає, що податок державі компанія має сплатити зі свого власного прибутку. 
Тому в системі звітності (PDF Report) фігурує колонка **"Tax Owed"** (Податок, який ми винні державі), що дає бізнесу чітке розуміння своїх фінансових зобов'язань.

### 2.3. Архітектура геопросторового аналізу (PostGIS)
Визначення юрисдикції за координатами — ресурсомістка задача. Перевірка тисяч багатокутників (кордонів округів) на рівні Node.js (Backend) призвела б до падіння продуктивності під час імпорту великих CSV файлів.
* **Рішення:** Вся математика перенесена на рівень бази даних. Використовується PostgreSQL із розширенням **PostGIS**. Координати замовлення перетворюються на точку (`ST_MakePoint`), і завдяки просторовим індексам (GiST) база швидко знаходить потрібні податкові юрисдикції (`ST_Covers`).

### 2.4. Генерація фінансових звітів та візуалізація (Heatmap)
Замість перевантаження фронтенду обчисленнями:
* **PDF-звіти:** Формуються виключно на бекенді (через агрегацію Prisma `_sum`, `_avg`) за допомогою бібліотеки `pdfkit-table`. Фронтенд просто завантажує готовий файл.
* **Теплова карта (Heatmap):** Щоб не передавати на клієнт сотні тисяч точок, бекенд оптимізує запит через просторове групування (віддає кластеризовані дані), які потім рендеряться через Leaflet.js.

---

## 🛠 3. Технологічний стек
- **Frontend:** Next.js (React), Tailwind CSS, React Leaflet (Heatmap), React-Select, Lucide Icons, PDF Kit .
- **Backend:** NestJS, TypeScript, Prisma ORM, PDFKit, Multer (для парсингу CSV).
- **Database:** PostgreSQL + розширення PostGIS (Supabase / Local PG).

---
## 🌐 4. Live Demo (Задеплоєна версія)

Проєкт вже розгорнутий і доступний онлайн (посилання також закріплено в описі цього репозиторію):

🔗 **Сайт:** [https://kyiv-hackaton.vercel.app/](https://kyiv-hackaton.vercel.app/)

**Дані для входу в систему:**
- **Email:** `qwe@qwe.com`
- **Пароль:** `123456`

> ⚠️ **УВАГА: ВАЖЛИВА ІНФОРМАЦІЯ ПРО COLD START!** > Бекенд проєкту задеплоєно на безкоштовному тарифі сервісу **Render**. Якщо до API не було звернень більше 15 хвилин, сервер "засинає".  
> **Перший запит (наприклад, натискання кнопки "Sign In") може зайняти до 50-60 секунд**, поки сервер не "прокинеться". Будь ласка, натисніть кнопку входу і просто трохи зачекайте — після цього система працюватиме блискавично! 🚀

## 🚀 5. Інструкція із запуску (Local Setup)

### Попередні вимоги:
- Встановлений **Node.js** (v18+)

### Крок 1. Створіть змінні середовища та встановіть залежності (Backend), запустіть бекенд
1. Перейдіть у папку бекенду:
   `cd backend`

2. Встановіть залежності
   `npm install`
   
3. Створіть файл .env з наступним вмістом:
   `DATABASE_URL="postgresql://postgres.oulncqilmvzqqpwhsnhl:71xmKkiRwx8uUZSp@aws-1-eu-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true"`
   `DIRECT_URL="postgresql://postgres.oulncqilmvzqqpwhsnhl:71xmKkiRwx8uUZSp@aws-1-eu-west-1.pooler.supabase.com:5432/postgres"`
   
5. Запустіть бекенд
   `npm run start:dev`

### Крок 2. Створіть змінні середовища та встановіть залежності (Frontend)
1. Створіть новий термінал та перейдіть у папку фронтенду:
   `cd frontend`

2. Створіть файл .env.local у папці Frontend з наступним вмістом:
   `NEXT_PUBLIC_API_URL="http://localhost:8080"`

3. Встановіть залежності
   `npm install` 

4. Запустіть фронтенд
   `npm run dev`

### Крок 3. Увійдіть в акаунт з наступними даними:
логін - qwe@qwe.com
пароль - 123456
